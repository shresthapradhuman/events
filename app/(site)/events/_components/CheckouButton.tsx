import { Button } from "@/components/ui/button";
import React from "react";

const CheckoutButton = () => {
  return  <Button className="mb-3 w-full">Get Tickets</Button>;
};

export default CheckoutButton;

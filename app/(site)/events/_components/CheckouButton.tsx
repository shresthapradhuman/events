import { Button } from "@/components/ui/button";
import React from "react";

const CheckoutButton = () => {
  return <Button className="w-full">Buy Tickets</Button>;
};

export default CheckoutButton;
